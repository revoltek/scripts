msin = 
msin.datacolumn = DATA
msin.baseline = [CR]S*&&

msout = 
msout.writefullresflag = False
# no dysco here, before need to fix weights

numthreads = 1 # bug fix

steps = [avg]

avg.type = averager
avg.freqstep = 1
avg.timestep = 1
